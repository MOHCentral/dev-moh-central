# =============================================================================
# SMF Forum Environment Configuration
# =============================================================================
# Copy to .env and customize for your environment:
#   LOCAL:     cp .env.local .env
#   PORTAINER: Use .env.portainer content in Portainer UI
# =============================================================================

# Compose Project Name (prefixes all containers/volumes/networks)
COMPOSE_PROJECT_NAME=mohaa-web

# MySQL Root Password
MYSQL_ROOT_PASSWORD=CHANGE_ME_ROOT_PASSWORD

# SMF Database
SMF_DB_NAME=smf
SMF_DB_USER=smf
SMF_DB_PASS=CHANGE_ME_SMF_PASSWORD

# MOHAA Stats API
# Internal URL (Docker network) - used by PHP to call API
# LOCAL:     http://host.docker.internal:8084
# PORTAINER: http://opm-stats-api:8080 (container name on shared network)
MOHAA_API_URL=http://opm-stats-api:8080

# Public URL - used by JavaScript in browser
# LOCAL:     http://localhost:8084
# PORTAINER: http://YOUR_SERVER_IP:8084 or https://api.yourdomain.com
MOHAA_API_PUBLIC_URL=http://localhost:8084
